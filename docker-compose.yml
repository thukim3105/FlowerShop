version: '3.8'

services:
  flowershop:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flowershop-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production